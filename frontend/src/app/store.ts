// This frontend currently uses React Context + local component state.
// Keep this file as a lightweight extension point for future state management needs.
export const store = {
  mode: 'context'
} as const;
